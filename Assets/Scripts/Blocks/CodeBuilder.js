#pragma strict

function Start () {
	//GetComponent(Codeblock).sayHi();

}

function Update () {

}

function createBlockObj(){
	//Create block model
	//<insert code here>

	//New block
	Debug.Log("Block object created");
	GetComponent(Codeblock).sayHi();
}
